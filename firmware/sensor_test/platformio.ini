; sensor_test, flash to the hub C3 to verify both right-side sensors at once.
[env:sensor_test]
platform = espressif32
board = seeed_xiao_esp32c3
framework = arduino
monitor_speed = 115200
build_flags =
    -DARDUINO_USB_CDC_ON_BOOT=1
lib_deps =
    adafruit/Adafruit BNO08x
