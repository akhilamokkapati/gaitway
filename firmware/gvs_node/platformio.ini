; Gaitway GVS node, the existing v1 ESP32 + DAC output stage.
; Classic ESP32 (has the DAC on GPIO25/26). Confirm the actual v1 board target.
[env:gvs_node]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
build_flags =
    -I include
